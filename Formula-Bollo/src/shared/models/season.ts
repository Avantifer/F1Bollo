export class Season {
  id: number;
  name: string;
  number: number;

  constructor(id: number, name: string, number: number) {
    this.id = id;
    this.name = name;
    this.number = number;
  }
}
