export class SideNavItem {
  icon: string;
  text: string;
  url: string;

  constructor(icon: string, text: string, url: string) {
    this.icon = icon;
    this.text = text;
    this.url = url;
  }
}
